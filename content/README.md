# Content data

このディレクトリには、果樹ページと品種ページに流し込むための構造化コンテンツを置きます。

- `fruits/*.md`: 果樹詳細ページ本文
- `cultivars/*.json`: 果樹に紐づく品種データ

現在のリポジトリにはアプリ本体とSupabaseスキーマが含まれていないため、DBカラムへ直接依存しない形で管理しています。実装側で `slug` をキーに取り込み、公開ページや管理画面の初期データとして利用してください。
